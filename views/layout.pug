doctype html
html(lang="en")
    head
        title Flash Cards 
    body
        include includes/header.pug
        if name
            h2 Welcome, #{name}! 
            // creates a goodbye button on the home page 
            form(action='/goodbye' method='post')
                button(type='submit') goodbye
        block content
        include includes/footer.pug
    